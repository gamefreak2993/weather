{
  "name": "weather",
  "version": "1.0.0",
  "description": "Weather is a small Node.js application that inputs an address from the weather and gets back the weather for that address.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.18.0",
    "request": "^2.88.0",
    "yargs": "^12.0.2"
  }
}
